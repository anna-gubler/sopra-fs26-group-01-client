/* Skill Map Type */

export interface SkillMap {
  id: number;
  title: string;
  description: string;
  numberOfLevels: number;
  isPublic: boolean;
  inviteCode: string;
  createdAt: string;
  updatedAt: string;
}